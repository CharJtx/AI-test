fastapi>=0.115.0
uvicorn>=0.32.0
httpx>=0.27.0
websockets>=14.0
python-dotenv>=1.0.0
Pillow>=10.0.0
python-multipart>=0.0.9
pyyaml>=6.0

# pydantic is already pulled in by fastapi — used by tools/distill_instant/
# schemas and services/skill_inference.
#
# Intentionally NOT listed here (would bloat the app image with ~3GB of CUDA
# wheels via torch):
#   sentence-transformers, torch, sqlite-vec, numpy, faster-whisper, yt-dlp
# These are only needed by tools/distill_fast/ which runs in its own
# dedicated docker images (tools/distill_fast/docker/Dockerfile.{embed,transcribe}).
# retrieve.py imports sentence_transformers / sqlite_vec lazily inside
# function bodies, so the app module is importable without them — RAG will
# only activate if a skill has a knowledge/ index AND those extras are
# pip-installed in the runtime.
